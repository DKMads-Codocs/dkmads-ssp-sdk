import UIKit

enum DKMadsCreativeChrome {
    /// 90% opaque black for fullscreen letterbox / interstitial chrome.
    static let letterboxBackgroundCss = "rgba(0,0,0,0.9)"
    static let letterboxBackgroundColor = UIColor(white: 0, alpha: 0.9)
}
